import React from 'react'

const StackButton = ({text}) => {
  return (
    <span className='text-xl md:text-3xl font-normal text-center text-gray-500'>{text}</span>
  )
}

export default StackButton
